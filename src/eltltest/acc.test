#!/bin/sh

. ./defs || exit 1

set -e
cat >prelude <<EOF
S=(
    0 1 \$0
    = 1
  )
EOF

cat >input <<EOF
include prelude
A=(
    0 1 \$2
    1 2 \$0
    = 0
  )
%
A(1,a,a|b)&S(1)
EOF
run 0 ./acc input || exit 1

cat >input <<EOF
A=(
  )
%
1
EOF
run 0 ./acc input || exit 1
